# AOS Discovery Engine v0.1

Upload these three files to the root of the AOS repository:

- index.html
- aos.css
- aos.js

The page guides a user through the AOS System Discovery Conversation and exports a structured JSON system definition.

To publish with GitHub Pages, use Settings → Pages → Deploy from a branch → main → /root.
